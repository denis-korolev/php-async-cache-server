services:
    cache-app-cli:
        build:
            context: docker
            dockerfile: development/php-cli/Dockerfile
        tty: true
        stdin_open: true
        volumes:
            - .:/app
        ports:
            - ${SERVER_PORT}:80
            - ${GATEWAY_PORT}:81
        env_file:
            - .env